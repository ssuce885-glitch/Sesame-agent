# Test

Summarize only preloaded workspace research artifacts.

Assertions:

- does not browse the web
- cites only local artifacts
- calls out stale snapshots instead of guessing
- keeps the result read-only
