Review workflow templates as distributable assets, not live runtime definitions.

Always validate:

- file names and library index references stay aligned
- trigger and owner_role fields match the template body
- descriptions stay concise and auditable
- required tools reflect current built-in runtime capabilities only
