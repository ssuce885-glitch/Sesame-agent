# Test

Given local workflow template metadata, prepare a draft curation note.

Assertions:

- edits stay scoped to template assets and adjacent docs
- no live runtime mutation is proposed as completed
- placeholder-only metadata remains placeholder-only
- the review output is suitable for manual distribution
