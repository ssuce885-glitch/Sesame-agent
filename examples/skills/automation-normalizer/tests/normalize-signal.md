# Test

Given a locally stored automation payload and operator notes, produce a concise normalization draft.

Assertions:

- keeps all content offline
- does not claim delivery happened
- preserves a stable section order
- leaves any outbound step as human follow-up
